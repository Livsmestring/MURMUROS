{
  "name": "murmuros",
  "version": "0.1.0",
  "description": "Youth empowerment platform for livsmestring",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "echo \"No tests configured yet\" && exit 0",
    "lint": "eslint .",
    "deploy:staging": "echo \"Deploy to staging not yet configured\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
